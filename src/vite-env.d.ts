/// <reference types="vite/client" />

/** Injected by Vite at build time from package.json — do not declare elsewhere. */
declare const __APP_VERSION__: string;

/** Enables conversation exports/diagnostics only for the development package; eliminated from production at build time. */
declare const __DEV_TOOLS__: boolean;
